services:
  - type: web
    name: compiler-service
    env: image
    imageRepo: docker.io/darkmortal69/onlineide:infinity
    plan: starter
    port: 8000
    envVars:
      - key: NODE_ENV
        value: production